@model IEnumerable<SelectListItem>
<div class="widget-body innerAll inner-2x">
    <div class="innerAll inner-2x">
        <ul class="chat media-list">
            @foreach (var item in Model.OrderByDescending(m => m.Text))
            {
                <li class="media">
                    <div class="media-body">
                        <span class="btn btn-success pull-right" onclick="loadModalContent('modal-help','EditService','Admin/God', '?serviceId=@(item.Value)')"><i class="fa fa-edit"></i> Редактировать услугу</span>
                        <h5 class="media-heading">@item.Text</h5>
                    </div>
                </li>
            }
        </ul>
    </div>
</div>

