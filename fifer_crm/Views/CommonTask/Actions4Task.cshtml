@model IEnumerable<SelectListItem>
@foreach (var item in Model)
{
    <input type="button" onclick="updateTaskTicket(@item.Value)" class="col-sm-6 btn btn-primary btn-action" value="@item.Text" />
}

